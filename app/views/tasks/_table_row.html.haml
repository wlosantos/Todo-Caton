%tr.border-top
  %td
    %span{:class => "#{task.cssStyle}"} 
      #{ task.symbol }
    - if task.done?
      %s.text-muted #{ task.description }
    - else
      #{ task.description }
  %td #{ l task.due_date }
  %td
    = link_to 'Editar', edit_task_path(task), class: 'button is-dark is-small'
    = link_to 'Apagar', task, method: :delete, class: 'button is-danger is-small', data: { confirm: 'Você tem certeza?' }
